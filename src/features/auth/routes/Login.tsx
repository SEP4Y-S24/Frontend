

import { Layout } from '../components/Layout';


export const Login = () => {
    //const navigate = useNavigate();

    return (
        <Layout title="Log in to your account">
            <p>login form</p>
        </Layout>
    );
};
